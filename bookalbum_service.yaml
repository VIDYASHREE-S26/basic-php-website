apiVersion: v1
kind: Service
metadata:
  labels:
    app: bookalbum_1
  name: bookalbum_1
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: bookalbum_1

